export class Movie {
  constructor(id, name, genres, releaseDate, poster) {
    this.id = id;
    this.name = name;
    this.genres = genres;
    this.releaseDate = releaseDate;
    this.poster = poster;
  }
}

export default Movie;
